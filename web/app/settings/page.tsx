export default function SettingsPage() {
  return (
    <section>
      <h1>Settings</h1>
      <p>Configure default model, global prompt, and user overrides.</p>
      <p>Coming next: UI for OpenAI key, model selection, and prompt editor.</p>
    </section>
  );
}
